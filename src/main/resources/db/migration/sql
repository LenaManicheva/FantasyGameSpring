create table character (
    id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name varchar,
    imageUrl varchar
);
create table person (
                     id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY ,
                     name varchar,
                     password varchar,
                     character_id int references character (id) On DELETE set null
);

Insert into character (imageurl,name)  values ('aliceblue.png', 'Alice in blue'),
                                               ('alicegreen.png', 'Alice in green'),
                                               ('card.png', 'The Card'),
                                               ('cheshirecat.png', 'The Cheshire Cat'),
                                               ('madhatter.png', 'The Mad Hatter'),
                                               ('umbrella.png', 'The Crazy Umbrella')